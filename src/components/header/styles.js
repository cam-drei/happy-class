import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  userNameView: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  userNameText: {
    fontSize: 16,
    marginRight: 5,
  },
  logo: {
    aspectRatio: 1,
    width: 30,
    height: 30,
    borderRadius: 30 / 2,
    marginRight: 10,
  },
});

export default styles;
